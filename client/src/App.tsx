// Scaffold placeholder. Replaced once the grid/page shell and first VoiceMeeter
// cards are built — see .claude/design/ELEMENTS.md for what's decided vs. open.
export default function App() {
  return (
    <div className="flex min-h-dvh items-center justify-center bg-background text-foreground">
      <div className="flex flex-col items-center gap-2 font-mono text-sm text-muted-foreground">
        <span className="text-base font-sans text-foreground">DecklingAir</span>
        <span>client scaffold — next up: the grid shell + first cards</span>
      </div>
    </div>
  );
}
